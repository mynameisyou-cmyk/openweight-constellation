{
  "name": "openweight-constellation-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --test --experimental-strip-types test/*.test.ts",
    "test:twice": "npm test && npm test",
    "deploy": "wrangler deploy --config wrangler.jsonc"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.1.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260730.1",
    "typescript": "5.9.3",
    "wrangler": "4.115.0"
  },
  "engines": {
    "node": ">=22"
  }
}
